# Frisk & Swing Dance Studio — dev revamp

Static dev site. **Do not deploy to production** without review.

## Stack
- Plain HTML/CSS/JS (no build step)
- Brand colors from logo: hot pink #E00080 + cyan #20A0E0

## Run locally
```
cd /home/harry/friskandswing && python3 -m http.server 8000
# then open http://localhost:8000
```
